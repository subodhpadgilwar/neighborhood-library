# Copy to .env.local for local development:
#   cp .env.example .env.local

# Internal URL used by Next.js server-side proxy to reach the backend.
# In Docker Compose this is the service name. Locally use http://localhost:8000.
INTERNAL_API_URL=http://localhost:8000
